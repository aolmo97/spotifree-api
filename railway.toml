[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
healthcheckTimeout = 100

[nixpacks]
install = ["python3", "ffmpeg", "python3-pip"]
setup = [
  "pip install spotdl"
]